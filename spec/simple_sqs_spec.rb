require 'spec_helper'

describe SimpleSqs do
  it 'has a version number' do
    expect(SimpleSqs::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
